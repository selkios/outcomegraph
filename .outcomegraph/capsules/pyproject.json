{
  "artifact_type": "capsule",
  "constraints": [
    "Preserve the setuptools build contract exactly: `requires = [\"setuptools>=68\", \"wheel\"]` and `build-backend = \"setuptools.build_meta\"`.",
    "Preserve project metadata exactly as captured: package name `outcomegraph`, version `0.1.0`, description `OutcomeGraph CLI and artifact reconciliation workflow`, readme `README.md`, and Python floor `>=3.11`.",
    "Regenerate runtime requirements and entry points exactly: dependencies `pydantic>=2,<3` and `typer>=0.24,<1`, module list `py-modules = [\"og\"]`, scripts `og = \"og:og_cli\"` and `ogd = \"og:ogd_cli\"`.",
    "Preserve pytest configuration `testpaths = [\"tests\"]` and `addopts = \"-q\"`.",
    "Preserve Ruff configuration `target-version = \"py311\"`, `line-length = 120`, and excludes `[\".venv\", \".outcomegraph\", \"__pycache__\"]`.",
    "Preserve Ty configuration for `environment.root`, `src.include`, `src.exclude`, and the explicit rule severities defined under `[tool.ty.rules]`."
  ],
  "created_at": "2026-03-06T19:59:05Z",
  "decision_refs": [
    "decisions/dec-pyproject-18e10014f6.json",
    "decisions/dec-pyproject-a875570fe0.json",
    "decisions/dec-pyproject-b580bd8219.json"
  ],
  "goal": "Capture the repository's Python packaging and tooling contract from the changed `pyproject.toml` snapshot for reproducible regeneration.",
  "id": "pyproject",
  "lineage": {},
  "materials_lock_ref": ".outcomegraph/materials.lock",
  "oracles": [
    {
      "command": null,
      "name": "pyproject-file-evidence",
      "scope": [
        "pyproject.toml",
        "build-system",
        "project metadata",
        "project scripts",
        "setuptools module config",
        "pytest config",
        "ruff config",
        "ty config",
        "no repository-native validation command evidenced in input payload"
      ]
    }
  ],
  "schema_version": 2,
  "scope": [
    "pyproject.toml",
    "Python packaging",
    "CLI entry points",
    "quality-tool configuration"
  ],
  "status": "success",
  "updated_at": "2026-03-06T22:35:27Z"
}
